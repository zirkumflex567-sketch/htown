{
  "name": "htown",
  "private": true,
  "workspaces": ["client", "server", "shared"],
  "scripts": {
    "dev": "concurrently \"npm:dev --workspace shared\" \"npm:dev --workspace server\" \"npm:dev --workspace client\"",
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace client",
    "lint": "npm run lint --workspace shared && npm run lint --workspace server && npm run lint --workspace client",
    "test": "npm run test --workspace shared && npm run test --workspace server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^9.9.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  }
}
