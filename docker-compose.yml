version: '3.9'
services:
  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - '2567:2567'
    environment:
      - NODE_ENV=production
      - JWT_SECRET=replace_me
    volumes:
      - server_data:/app/server/data
volumes:
  server_data:
