# Codex Prompt Suite — 3D Vehicle + Chase Cam

**Reihenfolge:** 00 → 16 (Dateinamen sind so sortiert, dass dein Dateibrowser sie korrekt ordnet).

## Empfehlung zur Abarbeitung
- In Codex: jeweils eine Datei komplett als Prompt benutzen.
- Nach jedem Schritt: `pnpm dev` starten, kurz prüfen, dann zum nächsten Prompt.
- Wenn ein Schritt sehr groß wird: in 2–3 Unter-Schritte splitten (max. ~10 Dateien pro Schritt).

Viel Erfolg!
